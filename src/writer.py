class DocumentWriter:
    def __init__(self):
        print("Document writer initialized.")

    def write_to_docx(self, text, output_path):
        # โค้ดสำหรับสร้างไฟล์ .docx
        print(f"Writing final text to {output_path}...")
        # doc = Document()
        # doc.add_paragraph(text)
        # doc.save(output_path)
        print("File saved successfully.")